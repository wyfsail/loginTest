<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fast.logintest.mapper.UserMapper">

<resultMap id="UserResult" type="com.fast.logintest.domain.User">
    <id column="user_id" property="userId"/>
    <result column="user_name" property="userName"/>
    <result column="password" property="password"/>
    <result column="gender" property="gender"/>
    <result column="avatar" property="avatar"/>
    <result column="create_time" property="createTime"/>
</resultMap>
    <insert id="insertUser">
        insert into user(user_name,password,gender,avatar,create_time)
        values(#{userName},#{password},#{gender},#{avatar},#{createTime})
    </insert>
    <update id="updateUser">
        update user set password = #{password} where user_id = #{userId}
    </update>
    <select id="selectUserByUsername" resultMap="UserResult">
    select * from user where user_name = #{username} limit 1
</select>

</mapper>